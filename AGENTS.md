# 项目协作说明

## 重要说明

- 开始实现前先给出整体方案；涉及架构、依赖、数据格式、IPC、系统权限或用户环境修改的重要决策，必须经用户审查后再落地。
- 全程使用中文沟通。代码标识符、协议字段和第三方专有名词保留其原始写法。
- 本项目是桌面与后台服务项目，目标平台为 Windows、Linux 和 macOS，架构覆盖 amd64 与 arm64；“跨平台”默认指这三个桌面平台，不包含移动端。
- 不得声称路由、代理绕行或节点直连已经生效，除非实际连接验证能够证明流量走了预期的物理接口和网关。
- 项目全程要使用中文！！
## 项目概览

CF Optimizer 是一个 Cloudflare 节点自动测速、优选和直连管理工具。它自主实现测速核心，不依赖旧版 CloudflareSpeedTest 可执行文件；负责更新 Cloudflare 官方网段、生成候选 IP、测量连接质量和下载速度、选择稳定节点，并通过系统路由与代理适配器维持直连策略。

目标进程模型：

```text
cf-optimizerd      特权后台服务
cf-optimizer-ui    普通权限 Wails 桌面界面
cf-optimizer       服务管理和诊断 CLI
```

桌面界面关闭后，后台服务和正在执行的任务应继续运行。UI 不得以管理员或 root 权限长期运行，也不得直接修改路由、Hosts 或代理配置。

## 技术栈

- 后台服务、CLI、测速核心：Go 1.23+
- 桌面容器：Wails
- 前端：React / TypeScript / Vite
- UI：Mantine，图标优先使用 Lucide
- 服务端数据：TanStack Query / TanStack Table
- 本地界面状态：Zustand
- 表单与校验：React Hook Form / Zod
- 图表：Apache ECharts
- 配置：YAML
- 本地通信：Windows Named Pipe / Unix Domain Socket
- 日志：Go `slog` 与文件轮换
- 测试：Go test / Vitest / Playwright

不要引入 Electron、Tauri、shadcn/ui 或移动端宿主，除非用户明确批准技术方案变更。

## 当前状态

项目处于第一阶段的基础能力建设期。目前已有初始 Go module，以及以下包：

- `internal/config`：配置默认值、加载、保存和严格校验。
- `internal/ranges`：Cloudflare 网段快照、远程更新、校验、缓存和回退。
- `internal/candidates`：可复现的 IPv4/IPv6 候选地址生成。
- `internal/fsutil`：原子文件写入与复制。
- `internal/store`：本地运行状态和历史摘要存储。

Wails 前端、CLI、后台服务、测速核心、平台网络层和代理适配器仍应按实施计划逐步建立。不要为了调用尚不存在的层而伪造空壳实现。

## 主要文档

- `CF-Optimizer-README.md`：项目定位、核心能力和技术栈摘要。
- `cf-optimizer-project-context.md`：需求、技术决策、架构边界和已知风险。
- `cf-optimizer-implementation-plan.md`：模块划分、开发阶段、测试计划和验收标准。

开始较大功能前应静默读取相关文档。若文档、测试和代码行为冲突，先查明当前实现与用户意图，不要自行选择一个版本覆盖其他版本。

## 常用命令

当前 Go 代码使用：

```powershell
go mod download
go fmt ./...
go vet ./...
go test ./...
go test -race ./...
```

`-race` 需要本机具备受支持的 CGO 工具链；环境不满足时应如实说明，并至少执行普通测试。前端建立后，以仓库实际的 `package.json` 和 Wails 配置为准，不要预设不存在的 pnpm 脚本。

## 目录约定

当前及目标代码结构如下：

- `cmd/daemon`：特权后台服务入口。
- `cmd/cli`：服务管理和诊断 CLI 入口。
- `cmd/desktop`：Wails 桌面入口。
- `internal/ranges`：Cloudflare 网段获取、校验、快照和回退。
- `internal/candidates`：候选 IP 生成。
- `internal/benchmark`：TCP、TLS、HTTPS 测速及结果模型。
- `internal/optimizer`：评分、迟滞、切换与故障回退。
- `internal/network/{windows,linux,darwin}`：网卡绑定、网关发现和路由事务。
- `internal/proxy`：统一策略模型及 Mihomo、sing-box、Xray、Generic、External 适配器。
- `internal/service`：Windows Service、systemd 和 LaunchDaemon 装配。
- `internal/ipc`：版本化本地 IPC、权限校验和事件流。
- `internal/config`：YAML 配置、默认值、校验和迁移。
- `internal/store`：状态、历史和回滚数据。
- `internal/diagnostics`：直连验证与诊断报告。
- `desktop/frontend`：React + Mantine 桌面界面。
- `packaging`：各平台安装、升级和卸载资源。
- `tests`：跨模块集成与端到端测试。

平台专属实现必须用文件边界和 Go build tags 隔离；共享算法、契约与数据模型不得依赖某个平台实现。

## 架构约束

- 保持“普通权限 UI -> Wails Bridge -> 受限本地 IPC -> 特权后台服务”的权限边界。
- 前端只能发送经过定义和校验的业务命令，不得直接调用 shell、系统路由、Hosts、代理配置文件或特权平台 API。
- 新增能力时先定义窄接口和领域契约，再实现服务、平台适配器、IPC 与 UI；不要把业务规则堆在 Wails binding 或 React 页面中。
- 后台服务必须校验所有 IPC 参数。前端校验只改善交互，不能替代服务端校验。
- 长任务通过可取消的 `context.Context` 和事件流报告进度；同一时间最多运行一个测速任务。
- 第三方代理适配器按代理内核而非客户端品牌划分。扩展进程使用版本化 JSON-RPC，不使用 Go 原生 plugin。
- 服务端数据由 TanStack Query 管理，Zustand 只保存页面、筛选和临时交互状态；不要复制同一份服务端状态。

## 网络与系统安全

- 测速代码不得读取或使用 `HTTP_PROXY`、`HTTPS_PROXY`、`ALL_PROXY` 等代理环境；连接必须使用显式 Dialer，并为 TLS 保留正确的 SNI 与 HTTP Host。
- 网卡绑定、临时路由和永久主机路由必须通过平台网络抽象实现。不要在共享业务层拼接 `route`、`ip`、PowerShell 或 shell 命令。
- 路由、Hosts 和代理策略修改必须具备计划、应用、验证、回滚和审计信息。切换时先应用并验证新节点，再移除旧策略。
- 写配置和状态应使用原子替换；修改第三方配置前必须备份，并使用带程序标记的受控区块，保证重复执行幂等。
- 启动时应能发现并恢复未完成的路由事务；任务取消、进程退出和卸载都不得遗留临时路由。
- Cloudflare 远程网段必须严格校验 CIDR、地址类别、数量和变化比例。异常更新不得覆盖最后一份有效快照。
- 日志记录关键阶段、决策、耗时、验证结果和回滚结果，但不得输出认证信息、完整隐私配置或其他敏感数据。
- 自动化测试默认不得修改开发机的真实路由、Hosts、服务或代理配置。此类验证应使用 mock、network namespace、虚拟机或用户明确批准的隔离环境。
- 不得尝试破坏或绕过第三方 VPN 的 Kill Switch 和安全策略；无法可靠直连时应报告限制。

## 编码约束

- 所有新增源码、配置、脚本和 Markdown 文档统一使用 UTF-8；除非既有文件或平台工具有明确要求，不引入其他编码。修改既有文件时保留其换行风格，避免无关的整文件换行变更。
- 命名应准确表达业务含义，避免无意义缩写、拼音、`data`、`info`、`manager` 等缺少上下文的宽泛名称。布尔值使用 `is`、`has`、`can`、`should` 等可判断语义。
- 一个包负责一个清晰领域，一个函数或方法只完成一个主要任务。复杂分支应拆成可测试的小步骤，避免超长函数、深层嵌套和跨层读写状态。
- 遵循高内聚、低耦合；业务核心依赖接口或稳定契约，平台、存储、网络和第三方代理细节放入适配层。
- 熟悉并合理使用设计模式，但不得为了模式而增加抽象。只有存在真实替换点、跨平台差异、复杂状态流转或重复逻辑时才抽取接口、策略、工厂或状态机。
- 所有新增或修改的非平凡函数、方法和导出声明必须有简短中文用途说明。注释解释意图、约束、边界和原因，不逐行翻译代码；复杂算法和系统权限操作应补充关键步骤注释。
- 禁止散落魔法数字、协议字符串、文件权限和超时值；使用有业务含义的常量或配置，并在定义处说明单位和适用范围。
- 不忽略错误，不使用空的 `catch` 或仅打印后继续执行。错误必须在当前层处理、转换或向上返回；降级和重试行为要显式、可观测且有上限。
- 不在生产代码中保留临时调试输出、注释掉的大段旧代码、无负责人 TODO 或未经说明的占位实现。
- 修改公共契约时同步更新实现、测试和相关文档；禁止只为通过测试而改变生产语义或写死测试数据。

## 日志约束

- Go 后台统一使用注入的结构化 `slog.Logger`，业务代码不得散用 `fmt.Print*`、标准库全局 `log` 或自行创建互不关联的 logger。前端也应通过统一日志封装输出。
- 正确使用日志级别：`Debug` 用于可关闭的诊断细节，`Info` 用于正常生命周期和重要状态变化，`Warn` 用于已降级或可恢复异常，`Error` 用于当前操作失败且需要处理的问题。
- 关键流程必须记录开始、结束、耗时和结果，包括配置加载与迁移、网段更新、候选生成、测速任务、节点选择与切换、路由事务、代理策略应用、后台服务生命周期、IPC 连接和崩溃恢复。
- 路由、Hosts 和代理配置修改必须记录计划、应用、验证和回滚四个阶段，并包含可关联的事务 ID；没有验证成功时不得记录为成功。
- 使用稳定的结构化字段，例如 `run_id`、`transaction_id`、`component`、`platform`、`adapter`、`interface`、`target_ip`、`duration`、`result` 和 `error`，不要把可检索信息拼进长字符串。
- 认证信息、Token、Cookie、授权头、IPC 密钥、用户完整代理配置和其他隐私数据不得写入日志。必要字段应脱敏；导出诊断报告前再次过滤敏感信息。
- 高频候选测试、重试和事件流不得逐条输出 `Info`。使用 `Debug`、采样、聚合统计或限流，避免日志风暴和测速性能失真。
- 错误通常只在能够决定重试、降级、回滚或向用户报告的责任边界记录一次；底层应包装并返回错误，避免同一错误在每层重复打印。
- 日志写入失败不得破坏核心流程，但应通过可用的备用通道报告。测试应验证关键事务日志和脱敏逻辑，不依赖易变的整行文本。

## Go 开发约束

- 遵循标准 Go 风格，提交前执行 `gofmt`。错误应携带清晰上下文，并保留可供 `errors.Is` / `errors.As` 判断的错误链。
- I/O、网络请求和长循环必须支持超时或取消；并发任务要有明确上限，避免 goroutine、Socket 和临时文件泄漏。
- 优先使用标准库和现有依赖。新增依赖前说明用途、维护状态、跨平台影响和可替代方案，并提交用户审查。
- 保持高内聚、低耦合。只有在确实存在替换点、跨平台差异或重复复杂逻辑时再引入接口和设计模式，避免过度抽象。
- 导出声明遵循 Go 文档注释规范；用途说明仍使用中文，并以被说明的标识符开头。
- 配置格式、IPC 协议和持久化状态必须版本化；发生兼容性变化时同时提供迁移或明确的兼容处理。

## 前端约束

- UI 使用 Mantine 现有组件和主题能力，优先复用组件，不另建一套相似基础控件。
- 界面定位为安静、紧凑、信息密度适中的桌面运维工具，不做营销落地页、装饰性大卡片或无意义动画。
- 状态、测速、路由、历史和日志页面应便于扫描、比较和重复操作；危险操作要显示影响范围、确认状态和最终验证结果。
- 运行时错误不得导致白屏；页面和长任务必须提供加载、空数据、取消、失败、部分成功与恢复状态。
- Windows、Linux 和 macOS 的 WebView、窗口尺寸和主题均需验证，不能用单一平台结果代替跨平台验收。

## 测试与验收

- 每次行为变更应补充与风险相称的测试。纯算法和校验逻辑优先写表驱动单元测试。
- 网段更新覆盖正常、`304 Not Modified`、备用源、异常数据、缓存回退和原子替换。
- 候选生成覆盖确定性、去重、IPv4/IPv6 比例、include/exclude、历史优先和边界地址。
- 网络与代理适配器覆盖计划、幂等应用、验证、失败回滚、崩溃恢复和配置刷新后的策略恢复。
- 修复缺陷时先添加能复现问题的测试，再修改实现；若无法自动化，应说明原因和人工验证步骤。
- 不能在当前系统验证的平台代码，至少要通过编译边界、平台隔离和可测试的共享契约检查，并明确列出剩余真机验证项。

## 生成物约束

- 不要提交 `dist/`、`bin/`、安装包、前端 `node_modules/`、覆盖率报告、Playwright 报告、运行时 `data/`、本地 `config.yaml`、Socket 文件或 IDE 配置。
- 不要手工修改 Wails、前端包管理器或其他工具生成的绑定与构建产物；应修改源定义后重新生成。
- 不清理或覆盖用户已有的未提交更改。发现与任务相关的并行修改时，应先理解并在其基础上工作。

## 代码提交约束

- 提交信息使用简明的 Conventional Commits 风格，并说明实际改动。
- 新需求以 `feat:` 开头，缺陷修复以 `fix:` 开头；文档、测试、重构和维护分别使用 `docs:`、`test:`、`refactor:`、`chore:`。
- 一个提交只包含一个逻辑变更，不混入无关格式化、生成物或本地配置。
